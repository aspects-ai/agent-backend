# Roadmap notes

Near-term priorities for the shared research room:

1. Semantic + keyword hybrid search across the document corpus.
2. Versioned history so agents can revert to a prior room state.
3. Multimodal ingestion (OCR / transcription) so images and PDFs become
   searchable via derived text.
